# Enable remote control for AI assistant (MCP server)
# Set to "true" to enable WebSocket connection to MCP backend
VITE_REMOTE_CONTROL_ENABLED=false

# WebSocket URL for remote control
# Default: ws://localhost:3000/remote-control
# VITE_REMOTE_CONTROL_WS=ws://localhost:3000/remote-control
